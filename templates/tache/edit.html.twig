{% extends 'gerant.base.html.twig' %}

{% block title %}Modification Tache{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="section">
            <h1>Modifier une tache</h1>
        </div>
        <div class="section d-flex justify-content-start">
            {{ include('tache/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
        <div class="section">
            <div class="btn btn-sm btn-dark">
                <a href="{{ path('projet_index') }}">Retourner à la liste</a>
            </div>
                {{ include('tache/_delete_form.html.twig') }}
            </div>
          
        </div>
    </div>
{% endblock %}
