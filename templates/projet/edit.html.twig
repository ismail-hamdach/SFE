{% extends 'base.html.twig' %}

{% block title %}Edit Projet{% endblock %}

{% block body %}


    <div class="container-fluid">
        <div class="section">
            <h1>Modifier un projet</h1>
        </div>
        <div class="section d-flex justify-content-start">
            {{include('projet/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
         <div class="section">
            <div class="btn btn-sm btn-dark">
                 <a href="{{ path('projet_index') }}">Retourner à la liste</a>
            </div>
            {{ include('projet/_delete_form.html.twig') }}
        </div>
          
    </div>
{% endblock %}
