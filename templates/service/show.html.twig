{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <div class="container-fluid">
            <div class="section">
                <h1>Service</h1>
    </div>
        <div class="section">
            <table class="table table-striped table-hover">
                <tbody> 
                    <tr>
                        <th>#</th>
                        <td>{{ service.id }}</td>
                    </tr>
                    <tr>
                        <th>Titre</th>
                        <td>{{ service.titre }}</td>
                    </tr>
                    <tr>
                        <th>Gérant</th>
                        <td>{{ service.gerant }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <div class="section">
            
            <div class="btn btn-sm btn-dark">
                <a href="{{ path('service_edit', {'id': service.id}) }}">edit</a>
            </div>
            
            <div class="btn btn-sm btn-dark">
                <a href="{{ path('service_index') }}">Retourner à la liste</a>
            </div>
            {{ include('service/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
