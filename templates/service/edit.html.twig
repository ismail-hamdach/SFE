{% extends 'base.html.twig' %}

{% block title %}Modification Service{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="section">
            <h1>Modification Service</h1>
        </div>
        <div class="section d-flex justify-content-start">
            {{ include('service/_form.html.twig', {'button_label': 'Enregistrer'}) }}
         </div>
         <div class="section">
            <div class="btn btn-sm btn-dark">
                <a href="{{ path('service_index') }}">Retourner à la liste</a>
             </div>
            {{ include('service/_delete_form.html.twig') }}
        </div>
          
    </div>
{% endblock %}
