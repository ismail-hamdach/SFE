{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-dark">{{ button_label|default('Rechercher') }}</button> 
    <input class="btn btn-primary btn-sm" type="reset" value="Effacer">
{{ form_end(form) }}
